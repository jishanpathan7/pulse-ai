/**
 * UI component library.
 *
 * Rule: exports only presentational primitives.
 * No Zustand, no WebSocket, no business logic, no data fetching.
 * Components are shadcn/ui base components + Tailwind.
 *
 * Components added via `pnpm dlx shadcn@latest add <component>`
 * targeting this package in Phase 3+ (Rendering).
 */

export { cn } from './utils.js';
